%input#features_order{:name => "features_order", :type => "hidden", :value => ""}/
Add a feature:
%select#add_feature{:name => "add_feature"}= options_for_select @available_features
%button{:onclick => "ContentEditor.addFeature($('add_feature').value); return false;"} Add Feature
%br/
%br/
#features
  - @content_model.content_model_features.each_with_index do |feature,idx|
    = render :partial => 'content_model_feature', :locals => { :feature => feature, :idx => idx }
:javascript
  ContentEditor.recreateFeatureSortables();
