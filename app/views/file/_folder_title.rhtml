<%= folder.ancestors.map { |fld| link_to(h(fld.id == 1 ? 'Root Folder'.t : fld.name),'javascript:void(0);', :onclick => "FileEditor.gotoFolder(#{fld.id});")  + " > " }.join.html_safe %>
<%=h(folder.id == 1 ? 'Root Folder'.t : folder.name) %>

